const redux = require("redux"); //This is how an import looks like a defalut node.js import statement for importing a third party package

const counterReducer = (state, action) => { //recieves state, and action by default, as state is the old state, action is the action by which it will do something =>Lecture264
    
}; 

const store = redux.createStore(); // =>should manage some data, determined by the reducer function, because it's the reducer function which will produce new state snapshots.


